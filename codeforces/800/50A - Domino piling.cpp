#include <bits/stdc++.h>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    
    int x, y;
    cin >> x;
    cin >> y;
    cout << (x * y) / 2 << endl;   
    
    return 0;
}